server:
  port: 8091

springfox:
  title: '@title@'
  version: '@version@'

fint:
  adapter:
    organizations: fake.no
    endpoints:
      sse: /sse/%s
      status: /status
      response: /response

  oauth:
    enabled: false
    access-token-uri: https://idp.felleskomponent.no/nidp/oauth/nam/token
    scope: fint-client
